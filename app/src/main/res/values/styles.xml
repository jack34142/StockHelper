<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>

        <item name="colorAccent">@color/colorSelected</item>
        <item name="android:textColorPrimaryDisableOnly">@color/colorSelected</item>

        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="colorBackgroundFloating">@color/colorDialogBackground</item>
        <item name="android:colorBackgroundFloating" tools:targetApi="23">@color/colorDialogBackground</item>

        <item name="android:textColor">@color/colorText</item>
        <item name="android:textColorPrimary">@color/colorText</item>
        <item name="android:textColorTertiary">@color/colorText</item>

    </style>

</resources>